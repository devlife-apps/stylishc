import {Style} from './types';

export const defaultStyle: Style = {
    avatarPadding: 10,
    avatarRadius: 50,
    avatarSize: 50,
    canvasColor: '#FFF0',
    canvasWidth: 900,
    strokeColor: ['#CCC'],
    strokeWidth: 2
};